@model List<MenuItem>

<div class="container">
    <h1 class="text-center">Välkommen till Restaurang Syb</h1>

    <div class="fs-6 text-center mx-auto" style="max-width: 900px;">
        På Restaurang SYB möts klassisk matkultur och moderna smaker i en varm och inbjudande miljö...
        Hos oss är det inte bara maten som står i centrum, utan även gemenskapen. Välkommen in och låt oss ta hand om resten!
    </div>

    <h2 class="text-center mt-4">Populära rätter</h2>


    <div class="row justify-content-center g-4">
        @foreach (var item in Model)
        {
            <div class="col-12 col-sm-10 col-md-6 col-lg-4 d-flex">
                <div class="card bg-secondary text-light w-100">
                    <img src="@item.ImageUrl" class="card-img-top" style="height:200px; object-fit:cover;" alt="@item.Title" />
                    <div class="card-body d-flex flex-column text-center">
                        <h5 class="card-title">@item.Title</h5>
                        <p class="card-text flex-grow-1">@item.Description</p>
                        <div class="mt-auto">
                            <span class="h5 text-warning">@item.Price kr</span>
                        </div>
                    </div>
                </div>
            </div>
        }
    </div>
</div>
